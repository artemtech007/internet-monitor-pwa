# 📱 Установка Internet Monitor PWA на телефон

## ✅ ЧТО РАБОТАЕТ (Протестировано):

### ✅ Android + Chrome (рекомендуется):
- **Открыть:** https://artemtech007.github.io/internet-monitor-pwa/
- **Подключиться** с токеном (PHONE001, PHONE002, PHONE003)
- **Нажать "📱 Установить PWA"** - появятся подробные инструкции
- **Следовать инструкциям:**
  - `⋮` (меню) → "Добавить на главный экран"
  - Диалог → "Добавить"
- **Результат:** Синяя иконка на рабочем столе ✅

### ⚠️ Firefox (ограниченная поддержка):
- **Работает** как веб-приложение
- **Service Worker не поддерживается** в инкогнито режиме
- **Установка PWA невозможна** в Firefox

### ⚠️ Chrome инкогнито (не рекомендуется):
- **Вызывает падение** всего браузера Chrome
- **Не использовать** для тестирования

## ❌ ЧТО НЕ РАБОТАЕТ:

### ❌ Автоматическая установка:
- **"+" в адресной строке** - не работает
- **"Установить приложение"** - не появляется автоматически
- **Требуется ручная установка** через меню браузера

### Firefox:

1. **Откройте приложение** по ссылке выше
2. **Подключитесь** к серверу
3. **Меню браузера:** `⋮` → `Установить эту страницу как приложение`
4. **Подтвердите** установку

## 🍎 Установка на iOS (Safari)

### Safari браузер:

1. **Откройте приложение:**
   ```
   https://artemtech007.github.io/internet-monitor-pwa/
   ```

2. **Подключитесь к серверу** (введите токен)

3. **Нажмите кнопку "Поделиться":**
   - Иконка 📤 внизу экрана
   - Выберите "На экран «Домой»" (Add to Home Screen)

4. **Подтвердите установку:**
   - Нажмите "Добавить"
   - Приложение появится на главном экране

## 🔧 Настройка работы в фоне

### Background Sync (Android)

PWA поддерживает автоматические тесты в фоне:

1. **Установите приложение** на главный экран
2. **Разрешите уведомления** (опционально)
3. **Приложение будет автоматически тестировать** скорость каждые 30 минут
4. **Результаты отправляются** в n8n webhook

### Wake Lock (предотвращение засыпания)

Во время ручных тестов:
- **Экран не гаснет** автоматически
- **Тесты выполняются** без прерываний
- **Экономит батарею** при длительных тестах

## 📋 Требования для работы в фоне

### Поддерживаемые браузеры:

#### ✅ Полная поддержка:
- **Chrome 80+** (Android)
- **Edge 80+** (Android)
- **Samsung Internet 12+**

#### ⚠️ Частичная поддержка:
- **Firefox 79+** (ограниченная background sync)
- **Safari 13.7+** (iOS) - базовая PWA

#### ❌ Нет поддержки:
- **Браузеры без Service Worker**
- **Устаревшие версии** браузеров

## 🔍 Проверка установки

### Android:
1. **Главный экран** - иконка "NetMonitor"
2. **Запуск** - открывается как отдельное приложение
3. **Меню приложения** - можно закрыть/обновить

### iOS:
1. **Главный экран** - иконка с названием "NetMonitor"
2. **Запуск** - открывается без браузерных элементов
3. **Многозадачность** - работает как обычное приложение

## 🛠️ Диагностика проблем

### Приложение не устанавливается:

**Android:**
```javascript
// В консоли браузера:
navigator.serviceWorker.getRegistrations().then(registrations => {
    console.log('Service Workers:', registrations.length);
});
```

**iOS:**
- Убедитесь что Safari обновлен
- Проверьте настройки приватности

### Не работает в фоне:

**Проверьте поддержку:**
```javascript
// В консоли:
console.log('Background Sync:', 'sync' in window.ServiceWorkerRegistration.prototype);
console.log('Periodic Sync:', 'periodicSync' in window.ServiceWorkerRegistration.prototype);
console.log('Wake Lock:', 'wakeLock' in navigator);
```

### Уведомления не приходят:

**Разрешите уведомления:**
```javascript
// Запрос разрешения:
Notification.requestPermission().then(permission => {
    console.log('Notification permission:', permission);
});
```

## 🎯 Возможности установленного PWA

### ✅ Работает как нативное приложение:
- **Оффлайн режим** (кеширование)
- **Быстрый запуск**
- **Фоновые задачи**
- **Push-уведомления** (будущие обновления)

### ✅ Улучшенный UX:
- **Полноэкранный режим**
- **Отсутствие браузерных элементов**
- **Иконка на рабочем столе**
- **Быстрые действия** (shortcuts)

### ✅ Фоновые возможности:
- **Background Sync** - автоматические тесты
- **Periodic Sync** - регулярные проверки
- **Wake Lock** - предотвращение засыпания
- **Push сообщения** - уведомления о проблемах

## 🔄 Обновление приложения

PWA обновляется автоматически:
1. **Service Worker** проверяет обновления
2. **При следующем запуске** - новая версия
3. **Можно принудительно обновить** в настройках браузера

## 📞 Поддержка

**При проблемах с установкой:**
1. Очистите кэш браузера
2. Перезагрузите страницу
3. Проверьте логи в консоли (F12)
4. Убедитесь в поддержке браузера
